{
	"name": "PerryRylance/DOMDocument",
	"description": "An extension of PHP's DOMDocument library. This library implements many common jQuery functions, adds support for HTML5, CSS selectors, method chaining and performing operations on sets of matched results. The library also helps facilitate server side form serialization and validation.",
	
    "require": {
		"php": "^5.4",
        "masterminds/html5": "^2.7",
        "symfony/css-selector": "^5.1"
    },
	"require-dev": {
        "phpdocumentor/phpdocumentor": "2.*"
    }
}
